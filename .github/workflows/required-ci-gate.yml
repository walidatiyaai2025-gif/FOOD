name: FOODEX Required CI Gate

on:
  pull_request:

permissions:
  contents: read

jobs:
  changes:
    name: Detect changed areas
    runs-on: ubuntu-latest
    outputs:
      backend: ${{ steps.filter.outputs.backend }}
      customer: ${{ steps.filter.outputs.customer }}
      driver: ${{ steps.filter.outputs.driver }}
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - id: filter
        name: Detect validation areas
        shell: bash
        run: |
          set -euo pipefail

          base="${{ github.event.pull_request.base.sha }}"
          head="${{ github.event.pull_request.head.sha }}"

          git diff --name-only "$base" "$head" > changed-files.txt
          cat changed-files.txt

          backend=false
          customer=false
          driver=false

          if grep -Eq '^(backend/|docs/api/openapi\.yaml$|\.github/workflows/backend-ci\.yml$)' changed-files.txt; then
            backend=true
          fi

          if grep -Eq '^(apps/customer_app/|packages/design_tokens/|scripts/package-mobile-artifact\.py$|scripts/tests/test_mobile_artifact\.py$|\.github/workflows/customer-app-ci\.yml$)' changed-files.txt; then
            customer=true
          fi

          if grep -Eq '^(apps/driver_app/|packages/design_tokens/|scripts/package-mobile-artifact\.py$|scripts/tests/test_mobile_artifact\.py$|\.github/workflows/driver-app-ci\.yml$)' changed-files.txt; then
            driver=true
          fi

          if grep -Eq '^(\.github/workflows/required-ci-gate\.yml|\.github/workflows/release-validation\.yml|scripts/release-readiness\.sh|docs/release/)' changed-files.txt; then
            backend=true
            customer=true
            driver=true
          fi

          {
            echo "backend=$backend"
            echo "customer=$customer"
            echo "driver=$driver"
          } >> "$GITHUB_OUTPUT"

  repository-policy:
    name: Repository Policy
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Validate repository foundation
        run: ./scripts/validate-repo.sh

      - name: Validate mobile artifact packaging
        run: python3 -m unittest discover -s scripts/tests -p 'test_mobile_artifact.py'

      - name: Require issue-style PR branch
        shell: bash
        run: |
          set -euo pipefail
          branch="${{ github.head_ref }}"
          [[ "$branch" =~ ^(feat|fix|chore|docs|refactor|test|release)/[0-9]+-[a-z0-9-]+$ ]]

  backend-validation:
    name: Backend validation
    needs: changes
    if: ${{ needs.changes.outputs.backend == 'true' }}
    uses: ./.github/workflows/backend-ci.yml

  customer-validation:
    name: Customer validation
    needs: changes
    if: ${{ needs.changes.outputs.customer == 'true' }}
    uses: ./.github/workflows/customer-app-ci.yml

  driver-validation:
    name: Driver validation
    needs: changes
    if: ${{ needs.changes.outputs.driver == 'true' }}
    uses: ./.github/workflows/driver-app-ci.yml

  required-ci-gate:
    name: required-ci-gate
    if: ${{ always() }}
    needs:
      - changes
      - repository-policy
      - backend-validation
      - customer-validation
      - driver-validation
    runs-on: ubuntu-latest
    steps:
      - name: Enforce applicable validation results
        shell: bash
        env:
          CHANGES_RESULT: ${{ needs.changes.result }}
          POLICY_RESULT: ${{ needs.repository-policy.result }}
          BACKEND_CHANGED: ${{ needs.changes.outputs.backend }}
          BACKEND_RESULT: ${{ needs.backend-validation.result }}
          CUSTOMER_CHANGED: ${{ needs.changes.outputs.customer }}
          CUSTOMER_RESULT: ${{ needs.customer-validation.result }}
          DRIVER_CHANGED: ${{ needs.changes.outputs.driver }}
          DRIVER_RESULT: ${{ needs.driver-validation.result }}
        run: |
          set -euo pipefail

          require_success() {
            local name="$1"
            local result="$2"
            if [[ "$result" != "success" ]]; then
              echo "::error::$name did not succeed (result: $result)"
              exit 1
            fi
          }

          require_applicable_success() {
            local changed="$1"
            local name="$2"
            local result="$3"

            if [[ "$changed" == "true" ]]; then
              require_success "$name" "$result"
            elif [[ "$result" != "skipped" && "$result" != "success" ]]; then
              echo "::error::$name had unexpected result for unchanged area: $result"
              exit 1
            fi
          }

          require_success "Change detection" "$CHANGES_RESULT"
          require_success "Repository Policy" "$POLICY_RESULT"

          require_applicable_success "$BACKEND_CHANGED" "Backend validation" "$BACKEND_RESULT"
          require_applicable_success "$CUSTOMER_CHANGED" "Customer validation" "$CUSTOMER_RESULT"
          require_applicable_success "$DRIVER_CHANGED" "Driver validation" "$DRIVER_RESULT"

          echo "All required validations for changed areas passed."
